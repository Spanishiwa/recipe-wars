import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#77c800"
      // dark: "#3949AB",
      // light: "#7986CB"
    },
    text: {
      primary: "#616E7C",
      dark: "#323F4B"
    },
    secondary: {
      main: "#f2f4f8"
      // dark: "#FF7100",
      // light: "#FFB402"
    }
  }
});

export default theme;
