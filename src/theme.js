import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#77c800"
      // dark: "#3949AB",
      // light: "#7986CB"
    },
    text: {
      primary: "#616E7C"
    },
    secondary: {
      main: "#E6CBCA"
      // dark: "#FF7100",
      // light: "#FFB402"
    }
  }
});

export default theme;
